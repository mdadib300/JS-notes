// numeric/number
let roll = 21;

// string
let schoolName = 'Baghapur High School and College - 108120';

// boolean
let isSingle = true;
let isMarried = false;

console.log(roll, 'is', typeof(roll), ',', schoolName, 'is', typeof(schoolName), ',', isSingle, 'and', isMarried,  'are', typeof(isSingle),)